{
  "name": "werewolf-cf",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "check:wrangler": "node scripts/check-wrangler-config.mjs",
    "predeploy": "npm run check:wrangler -- --production",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260423.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.14.4"
  }
}
