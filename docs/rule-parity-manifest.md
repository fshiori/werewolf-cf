# Rule Parity Manifest

Date: 2026-05-06

Reference target: `ref/diam1.3.61.kz_Build0912`

Purpose: map the PHP rule surface to the current Cloudflare TypeScript implementation and tests. This is a tracking artifact; it records rule coverage and known gaps without copying or modifying reference files.

## Status Key

- Implemented: covered by implementation and focused automated tests.
- Partial: implemented for the main path, but PHP has additional branches, presentation variants, or edge cases still unmapped.
- Missing: no equivalent implementation yet.

## Prompt-To-Artifact Checklist

| Reference area | PHP source | TypeScript artifact | Test evidence | Status | Notes / remaining work |
| --- | --- | --- | --- | --- | --- |
| Role deck thresholds | `game_functions.php`, `room_manager.php` | `src/game.ts` `REFERENCE_ROLE_DECKS`, `startGame` | `tests/game.test.ts` role deck tests for 3/5/6/7/8/13/15 players plus large-player count snapshots for 16/20/23/25/28/30 players | Implemented | Core threshold and large deck counts are pinned; add more snapshots only if a future PHP extraction finds variant decks. |
| Optional roles: poison, big wolf, authority, decider, lovers, betrayer, child fox, two foxes, cat | `room_manager.php`, `game_functions.php` | `src/game.ts` `applyRoomOptions` and viewer-specific partner helpers | `tests/game.test.ts` option and self-excluded partner-list tests for poison, big wolf, authority/decider, lovers, betrayer, child fox, two foxes, cat; `tests/room.test.ts` role partner websocket lists | Implemented | `docs/reference-betrayer-cult-extraction.md` records that the reference `cult` image names are used for the `betr` / `背德` role, not a separate playable cult role; partner lists follow the reference `uname <> self` display. |
| Wish role | `user_manager.php`, `game_vote.php` | `src/game.ts` wish-role handling in `startGame`; `src/validation.ts` optional-role wish validation; `src/render.ts` room wish-role selector; `docs/reference-wish-role-extraction.md` | `tests/game.test.ts` core, randomized duplicate-conflict, and enabled optional-role wish tests; `tests/validation.test.ts` wished-role validation; `tests/render.test.ts` wish selector options | Implemented | Core and enabled optional-role preferences exist; the port now follows reference-style randomized wish processing and intentionally rejects the PHP index-0 loose-comparison quirk as an implementation bug rather than a game rule. |
| Dummy boy first night | `game_play.php`, `game_functions.php` | `src/game.ts` dummy player and first-night target rule; `src/room-client.ts` first-night action prompt/reminder gating | `tests/game.test.ts` dummy-boy first-night and non-wolf first-night action rejection tests; `tests/room.test.ts` dummy status sync and websocket non-wolf first-night action rejection tests; `tests/render.test.ts` first-night role-action gating hooks | Implemented | Includes custom dummy name, last words, forced dummy target, and blocks non-wolf night role actions/prompts during the dummy-boy first night. |
| Day vote, revote, authority, decider, self-vote | `game_play.php`, `game_functions.php`, `setting.php` | `src/game.ts` `castDayVote`, `resolveDay`, vote target helpers, 10-tie draw handling; `src/room-client.ts` live observer/dead vote panel, revote prompt, and post-vote action disabling; `src/render.ts` old-log vote round totals, per-ballot target-count column, and reference-inspired vote table classes | `tests/game.test.ts` authority, decider, self-vote, duplicate-vote rejection, day-to-night, 10-tie draw tests; `tests/room.test.ts` vote validation and duplicate-vote websocket rejection tests; `tests/render.test.ts` observer/dead vote panel, live revote prompt, post-vote action disabling hooks, and transcript vote-total/style artifact checks | Implemented | Vote rule behavior is covered, including one vote per round until revote/reset clears the ballot map and the reference `revote_draw_times = 10` draw result; exact PHP table styling variants remain a visual parity gap. |
| Open vote and vote status visibility | `game_functions.php` vote output | `src/messages.ts`, `src/room.ts`, `src/room-client.ts`, `src/render.ts` | `tests/messages.test.ts` vote visibility flags, viewer-specific own-vote mappings, viewer-specific own night-action targets, mappings/status tests; `tests/room.test.ts` websocket hidden-vote self-target and night-action self-target broadcast tests; `tests/render.test.ts` voted player styling, observer/dead vote panel, live open-vote observer table, old-log aggregate/per-row vote total/style tests, voter received-count column, PHP-style `投票給 N 票 →` phrase, `N 日目 ( M 回目)` heading, bordered zero-spacing table attributes, and bold voter/target cells | Partial | Public message privacy, live observer/dead status, explicit live `openVote`/`voteStatus` flags, viewer-specific hidden-vote self mapping/websocket delivery, viewer-specific own night-action target mapping/websocket delivery, live open-vote observer rows, historical per-target vote totals, old-log per-ballot target counts, voter received-count display, PHP-style vote phrase, PHP-shaped round heading, and reference-inspired old-log vote-table attributes/coloring are covered; exact broader visibility-mode/authenticated-view variants remain partial. |
| Night kill | `game_play.php`, `game_functions.php` | `src/game.ts` `castNightKill`, `resolveNight`; `src/room-client.ts` selected-target prompt/action disabling | `tests/game.test.ts` wolf-only, duplicate wolf action rejection, one wolf-pack action completion, fox attack, poison kill, big wolf, dummy tests; `tests/room.test.ts` night kill validation/persistence, wolf-pack websocket completion, and duplicate websocket rejection tests | Implemented | Includes target aggregation, the reference one wolf-pack vote completion rule, duplicate submission rejection by the acting wolf, and dummy first-night restriction. |
| Seer and big wolf divination | `game_play.php`, `game_functions.php` | `src/game.ts` `castDivination`, `divinationResultForRole`; `src/room-client.ts` first-night prompt/reminder gating | `tests/game.test.ts` seer, big wolf odds, first-night rejection, fox death/last words tests; `tests/room.test.ts` seer websocket completion and first-night rejection tests | Implemented | Random odds are pinned with deterministic random values; dummy-boy first night is blocked consistently with the required-action set. |
| Child fox divination | `game_play.php`, `game_functions.php` | `src/game.ts` `castChildFoxDivination`, `childFoxDivinationResult`; `src/room-client.ts` first-night prompt/reminder gating | `tests/game.test.ts` child fox action, first-night rejection, and child-fox fox-win tests; `tests/room.test.ts` child fox websocket completion and first-night rejection tests | Implemented | Failure odds are deterministic in focused tests; dummy-boy first night is blocked consistently with the required-action set. |
| Guard | `game_play.php`, `game_functions.php` | `src/game.ts` `castGuard`, guarded night resolution; `src/room-client.ts` prompt/reminder gating | `tests/game.test.ts` guard role, first-night rejection, and protected kill tests; `tests/room.test.ts` guard authorization/persistence and first-night rejection tests | Implemented | Consecutive target restrictions are not modeled unless a future PHP extraction shows them in this build. |
| Cat revive and cat survival | `game_play.php`, `game_functions.php`, `game_vote.php` | `src/game.ts` `castCatRevive`, `pickCatRevival`, cat attack survival, optional night-action completion; `src/room-client.ts` revive-target prompt and required-reminder split | `tests/game.test.ts` cat option, first-night rejection, revive/survival/poison tests, optional revive completion, and no sudden-death for skipped revive; `tests/room.test.ts` cat action authorization/persistence and first-night rejection tests; `tests/render.test.ts` optional cat prompt versus required reminder hooks | Implemented | Random odds are pinned with deterministic random values; live prompts require an actual eligible dead target. Reference `CAT_DO` can be submitted but is not counted in the required night-action total, so skipped revive does not block dawn or trigger sudden death. |
| Poison and linked deaths | `game_functions.php` victory/death helpers | `src/game.ts` `resolveDay`, `resolveNight`, `applyLinkedDeaths` | `tests/game.test.ts` poison execution, poison night kill, cat-as-poison, lover/betrayer linked-death tests | Implemented | Exact random target selection is deterministic in tests. |
| Win conditions: villagers, wolves, foxes, lovers, draw | `game_functions.php` victory helpers | `src/game.ts` `determineWinner`, `withWinOrNextDay`, `resolveDay` draw handling | `tests/game.test.ts` wolf win, fox win, child-fox fox win, big-wolf + child-fox wolf/fox priority tests, lovers cross-faction four-player threshold, same-faction lover non-win, more-than-four lover non-win, one-lover non-win, big-wolf counting tests, revote draw tests, and all-dead sudden-death draw tests | Implemented | Lovers follow the reference `total_count <= 4`, exactly two living lovers, different main-faction rule. Big-wolf + child-fox rooms follow the reference wolf-win branch where a lone child fox does not steal wolf victory unless enough real fox/big-wolf presence remains. Draw is represented as a first-class winner for repeated tied votes, all-dead sudden death, and GM declaration. Additional rare PHP edge cases can be added as discovered. |
| Last words | `game_play.php`, `game_functions.php` | `src/game.ts` `setLastWords`, death logs; `src/room.ts` websocket handler | `tests/game.test.ts` last words after execution/night/fox death; `tests/room.test.ts` websocket last-words tests | Implemented | UI presentation is current-port style, not exact PHP layout. |
| Objection | `game_play.php`, SWF notification paths, `ROOMEND` protest vote | `src/game.ts` `raiseObjection` and `requestRoomEnd`; `src/messages.ts` public objection counts and same-day room-end voter filtering; `src/render.ts` reference objection image button, legacy `廢` control, and transcript labels for room-end requests; `src/room-client.ts` remaining-count controls, room-end command, and browser beep | `tests/game.test.ts` objection limits and room-end majority threshold; `tests/room.test.ts` objection persistence and room-end websocket finalization; `tests/messages.test.ts` objection count and same-day room-end voter filtering; `tests/validation.test.ts` `room_end_vote`; `tests/render.test.ts` reference image/count, room-end command, room-end transcript label, and sound controls | Partial | Command, remaining-count UI, reference image chrome, browser beep, reference-style same-day majority room-end request, and old-log transcript labeling now exist; SWF playback is intentionally replaced by browser audio. |
| Private channels: wolf, fox, common, lovers, dead, self-talk | `game_play.php`, `game_functions.php` talk locations | `src/game.ts` channel guards; `src/room.ts` filtered broadcasts and transcript persistence | `tests/game.test.ts` channel permission tests; `tests/room.test.ts` private broadcast, common voice, dead chat, self-talk tests | Implemented | Viewer-specific old-log masking remains partial. |
| GM controls and whispers | `game_play.php`, `game_vote.php` `GMVote` / `GM_CHANNEL`, admin/GM paths | `src/room.ts`, `src/game.ts` force controls, common-voice toggle, GM-kill vote/action reset, life/role-change pending-action cleanup, and `chdis` channel restriction persistence; `src/index.ts` room-summary parsing, room-admin status filters, runtime-config admin writes, and admin menu routing; `src/render.ts` admin menu/room/config context plus PHP-style transcript speaker labels for `自言自語` and `GM → 目標`; `src/room-client.ts` GM controls and live GM action panel | `tests/game.test.ts` force-end/role/life/pending-action cleanup/GM-kill reset/flag/channel restriction tests; `tests/room.test.ts` GM websocket, whisper, GM-kill reset, life/role-change pending-action cleanup, common-voice, and channel-restriction tests; `tests/index.test.ts` room option/admin parsing, active/ended/all filters, admin menu, and config admin update tests; `tests/render.test.ts` room-list/admin/config markers, live GM action panel markers, and transcript self-talk/GM-whisper speaker labels | Partial | Core controls, common voice, room summaries, filtered admin waste-room context, runtime config administration, admin tool navigation, live GM action panel/status, GM kill resets pending votes/actions like the reference `GM_KILL`, role changes clear pending actions involving the adjusted player, legacy-style `chdis:ch_wolf:ch_common:ch_lovers:ch_fox` restriction matrix, and PHP-shaped self-talk / GM-whisper transcript labels exist; broader server-management screens and exact authenticated GM presentation remain partial. |
| Timed phases, silence, sudden death | `game_play.php` flow checks | `src/game.ts` alarms, silence acceleration, warning/death handling; `src/messages.ts` warning timestamp; `src/room-client.ts` phase/revote/warning beep | `tests/game.test.ts` timer defaults, silence acceleration, realtime no-acceleration, sudden-death warnings; `tests/room.test.ts` DO alarm tests; `tests/messages.test.ts` warning timestamp; `tests/render.test.ts` state sound hooks | Implemented | Legacy SWF/cookie playback remains intentionally not exact. |
| Lobby start, leave, kick votes | `user_manager.php`, `game_vote.php`, `room_manager.php` resident handling | `src/game.ts` lobby vote/leave/kick helpers, reference 8-player resident-start minimum and dummy-boy start-vote credit; `src/messages.ts` filtered lobby kick-vote status; `src/room.ts` websocket commands and reference-style leave/kick reset logs; `src/room-client.ts` lobby kick-vote target status | `tests/game.test.ts` lobby host/start/kick/leave, start-vote minimum, and dummy-boy start-vote credit tests; `tests/messages.test.ts` filtered lobby kick-vote status; `tests/room.test.ts` websocket lobby start/kick/leave, direct kick reset-log, vote-kick reset-log, lobby leave reset-log, and dummy-boy resident start-vote tests; `tests/render.test.ts` lobby kick-vote status rendering hooks | Partial | Current implementation covers resident start/kick/leave, the reference start-vote minimum, dummy-boy vote credit, filtered live kick-vote target status, and leave/kick success vote reset/logging for direct leave, direct kick, and resident-vote kick paths; deeper exact authenticated lobby presentation remains partial. |
| D1 records and player stats | `game_functions.php` old log/stat paths | `src/room.ts` `syncRoomStatus`, `src/index.ts` record/stat routes | `tests/room.test.ts` final record/stat sync; `tests/index.test.ts` records/profile/leaderboard tests | Implemented | Database writes are batched at game end as required by the Cloudflare port constraints. |
| Betrayer/cult asset naming | `setting.php`, `index.php`, `game_functions.php`, `game_vote.php`, `lang/jpn/rule.php` | `src/types.ts` `betrayer`; `src/game.ts` `applyRoomOptions`, linked deaths; `src/render.ts`, `src/room-client.ts` role icon/label | `tests/game.test.ts` betrayer option and linked-death tests; `tests/render.test.ts` betrayer label/icon tests; `docs/reference-betrayer-cult-extraction.md` | Implemented | Reference `role_cult.gif`/`role_cult_partner.gif` are assigned to `$role_betr_image`; no separate cult rule path was found in this build. |
| Viewer-specific transcript masking | `game_functions.php` talk output branches | `src/index.ts` private-event filtering and explicit transcript viewer query validation; `src/render.ts` transcript grouping/styling plus `viewer=public/player/dead/gm` masks, record/event-derived player-view selectors, role-derived player channel visibility, state-preserving old-log display links, PHP-style player-view old-log/game-log links, GM channel-control system rows, and explicit-system event classification for private rows without player ids | `tests/index.test.ts` private event hidden until ended and transcript viewer query tests, including required `viewer_player_id`, player-view links, and teammate channel masking; `tests/render.test.ts` transcript location styling, viewer-mode filtering, no-player-id private-row masking, player selector rendering, role-derived teammate channel visibility, state-preserving transcript links, PHP-style player-view alias links, and heaven-only GM operation visibility | Partial | Additive viewer modes now cover public, own-player private rows, same-team wolf/fox/common/lovers channel rows derived from saved records, addressed GM whispers, dead/heaven, and GM transcript perspectives; player mode requires an explicit viewer id, private rows without player ids are not treated as system-visible rows, and the page exposes direct modern and PHP-style player-view choices, but exact PHP layout/authenticated identity semantics remain partial. |
| Visual/browser parity | `game_view.php`, `game_play.php`, `index.php` | `src/render.ts`, `src/room-client.ts`, `docs/visual-parity-checklist.md` | `scripts/smoke-local-ui.mjs`, `tests/local-ui-smoke.test.mjs` | Partial | Manual/browser capture matrix exists; no browser or screenshot baseline is available in this environment. |

## Verification Gates

Current parity work should continue to pass:

- `npm test`
- `npm run typecheck`
- `npm run check:wrangler`
- `npm run check:whitespace`
- `npm run check:d1-schema`

## Next Rule Work

1. Add focused tests for any newly discovered victory edge cases from `game_functions.php`.
2. Decide whether viewer-specific old-log masking should be implemented as separate endpoints/modes or as parameters on `/room/:roomId/log`.
3. Run `docs/visual-parity-checklist.md` and add screenshot-based UI parity once a browser is available.
