# Rule Parity Manifest

Date: 2026-05-07

Reference target: `ref/diam1.3.61.kz_Build0912`

Purpose: map the PHP rule surface to the current Cloudflare TypeScript implementation and tests. This is a tracking artifact; it records rule coverage and known gaps without copying or modifying reference files.

## Status Key

- Implemented: covered by implementation and focused automated tests.
- Partial: implemented for the main path, but PHP has additional branches, presentation variants, or edge cases still unmapped.
- Missing: no equivalent implementation yet.

## Prompt-To-Artifact Checklist

| Reference area | PHP source | TypeScript artifact | Test evidence | Status | Notes / remaining work |
| --- | --- | --- | --- | --- | --- |
| Role deck thresholds | `game_functions.php`, `room_manager.php` | `src/game.ts` `REFERENCE_ROLE_DECKS`, `startGame` | `tests/game.test.ts` role deck tests for 3/5/6/7/8/13/15 players plus large-player count snapshots for 16/20/23/25/28/30 players | Implemented | Core threshold and large deck counts are pinned; add more snapshots only if a future PHP extraction finds variant decks. |
| Medium readings | `game_play.php`, `game_vote.php` `NECROMANCER_RESULT` | `src/game.ts` `mediumReadingForPlayer`/`mediumReadingsForPlayer`, reference role-result mapping; `src/room.ts` multi-result websocket delivery; `src/room-client.ts` live result labels | `tests/game.test.ts` living-medium visibility, special big-wolf/child-fox result tests, and multiple day sudden-death reading carryover; `tests/room.test.ts` multi-result websocket delivery; `tests/messages.test.ts` medium result payload test | Implemented | Matches the reference result categories for `human`, `wolf`, `wfbig`, and `fosi`; multiple same-day `NECROMANCER_RESULT` entries from day sudden deaths plus execution are preserved and remain private to living mediums during the next day. |
| Optional roles: poison, big wolf, authority, decider, lovers, betrayer, child fox, two foxes, cat | `room_manager.php`, `game_functions.php` | `src/game.ts` `applyRoomOptions` and viewer-specific partner helpers; `src/index.ts` legacy `room_manager.php` create-form option mapping | `tests/game.test.ts` option and self-excluded partner-list tests for poison, big wolf, authority/decider, lovers, betrayer, child fox, two foxes, cat; `tests/index.test.ts` legacy room-manager create-form option mapping; `tests/room.test.ts` role partner websocket lists | Implemented | `docs/reference-betrayer-cult-extraction.md` records that the reference `cult` image names are used for the `betr` / `背德` role, not a separate playable cult role; partner lists follow the reference `uname <> self` display, and legacy room creation maps reference `option_role_*` / `game_option_*` fields into the same serialized D1 option set. |
| Wish role | `user_manager.php`, `game_vote.php` | `src/game.ts` wish-role handling in `startGame`; `src/validation.ts` optional-role wish validation; `src/render.ts` room wish-role selector; `docs/reference-wish-role-extraction.md` | `tests/game.test.ts` core, randomized duplicate-conflict, and enabled optional-role wish tests; `tests/validation.test.ts` wished-role validation; `tests/render.test.ts` wish selector options | Implemented | Core and enabled optional-role preferences exist; the port now follows reference-style randomized wish processing and intentionally rejects the PHP index-0 loose-comparison quirk as an implementation bug rather than a game rule. |
| Dummy boy first night | `game_play.php`, `game_functions.php` | `src/game.ts` dummy player and first-night target rule; `src/room-client.ts` first-night action prompt/reminder gating | `tests/game.test.ts` dummy-boy first-night and non-wolf first-night action rejection tests; `tests/room.test.ts` dummy status sync and websocket non-wolf first-night action rejection tests; `tests/render.test.ts` first-night role-action gating hooks | Implemented | Includes custom dummy name, last words, forced dummy target, and blocks non-wolf night role actions/prompts during the dummy-boy first night. |
| Day vote, revote, authority, decider, self-vote | `game_play.php`, `game_functions.php`, `setting.php` | `src/game.ts` `castDayVote`, `resolveDay`, vote target helpers, 10-tie draw handling; `src/room-client.ts` live observer/dead vote panel, revote prompt, post-vote action disabling, and self-vote-enabled target controls in both the main player list and legacy `game_vote.php` target list; `src/render.ts` old-log vote round totals, per-ballot target-count column, transcript vote-round payload labels, and reference-inspired vote table classes | `tests/game.test.ts` authority, decider, self-vote, duplicate-vote rejection, day-to-night, 10-tie draw tests; `tests/room.test.ts` vote validation and duplicate-vote websocket rejection tests; `tests/render.test.ts` observer/dead vote panel, live revote prompt, post-vote action disabling hooks, self-vote-enabled client target checks, and transcript vote-total/round/style artifact checks | Implemented | Vote rule behavior is covered, including one vote per round until revote/reset clears the ballot map, UI target controls honoring the self-vote room option during day phase, and the reference `revote_draw_times = 10` draw result; exact PHP table styling variants remain a visual parity gap. |
| Core game lifecycle | `game_play.php`, `game_vote.php`, `game_functions.php` phase loop | `src/game.ts` `startGame`, `castDayVote`, `castDivination`, `castNightKill`, win resolution, medium result delivery | `tests/game.test.ts` continuous opening-day-to-village-victory lifecycle test | Implemented | Covers one continuous game path through first-day execution, required night actions, second-day medium reading, wolf execution, and final village victory. |
| Open vote and vote status visibility | `game_functions.php` vote output | `src/messages.ts`, `src/room.ts`, `src/room-client.ts`, `src/render.ts`, `src/index.ts` safe legacy vote POST redirect | `tests/messages.test.ts` vote visibility flags, viewer-specific own-vote mappings, viewer-specific own voted-player self state, viewer-specific own night-action targets, hidden-status own night-action self state, mappings/status tests; `tests/room.test.ts` websocket hidden-vote self-target and hidden-vote self-state broadcast tests; `tests/index.test.ts` legacy vote POST alias tests including `auto_reload` preservation; `tests/render.test.ts` voted player styling, observer/dead vote panel, night observer action-status label, live open-vote observer table, legacy `game_vote.php` command/situation map, legacy vote form shell and state-synced target-list/hidden-field/selector/submit script, old-log aggregate/per-row vote total/style tests, voter received-count column, PHP-style `投票給 N 票 →` phrase, `N 日目 ( M 回目)` heading, bordered zero-spacing table attributes, and bold voter/target cells | Partial | Public message privacy, live observer/dead status, night observer/dead action progress wording, explicit live `openVote`/`voteStatus` flags, viewer-specific hidden-vote self mapping/websocket delivery, viewer-specific own voted-player state even when vote-status is hidden, viewer-specific own night-action target and completion state even when vote-status is hidden, live open-vote observer rows, `game_vote.php` now documents and renders a reference-shaped `command=vote` form shell with lobby/day/night `situation` values, hidden `room_no`/`situation`/`vote_times`/target fields and the disabled situation selector synced from the selected command and live revote count, state-synced target-list chrome, server-rendered start-vote fallback chrome, selected-target submit handling, safe legacy POST redirects back to the vote panel while preserving normalized `auto_reload` and WebSocket actions remain DO-validated, back link, submit labels, historical per-target vote totals, old-log per-ballot target counts, voter received-count display, PHP-style vote phrase, PHP-shaped round heading, and reference-inspired old-log vote-table attributes/coloring are covered; exact broader visibility-mode/authenticated-view variants remain partial. |
| Night kill | `game_play.php`, `game_functions.php` | `src/game.ts` `castNightKill`, `resolveNight`; `src/room-client.ts` selected-target prompt/action disabling | `tests/game.test.ts` wolf-only, duplicate wolf action rejection, one wolf-pack action completion, fox attack, poison kill, big wolf, dummy tests; `tests/room.test.ts` night kill validation/persistence, wolf-pack websocket completion, and duplicate websocket rejection tests | Implemented | Includes target aggregation, the reference one wolf-pack vote completion rule, duplicate submission rejection by the acting wolf, and dummy first-night restriction. |
| Seer and big wolf divination | `game_play.php`, `game_functions.php` | `src/game.ts` `castDivination`, `divinationResultForRole`; `src/room-client.ts` first-night prompt/reminder gating | `tests/game.test.ts` seer, big wolf odds, first-night rejection, fox death/last words tests; `tests/room.test.ts` seer websocket completion and first-night rejection tests | Implemented | Random odds are pinned with deterministic random values; dummy-boy first night is blocked consistently with the required-action set. |
| Child fox divination | `game_play.php`, `game_functions.php` | `src/game.ts` `castChildFoxDivination`, `childFoxDivinationResult`; `src/room-client.ts` first-night prompt/reminder gating | `tests/game.test.ts` child fox action, first-night rejection, and child-fox fox-win tests; `tests/room.test.ts` child fox websocket completion and first-night rejection tests | Implemented | Failure odds are deterministic in focused tests; dummy-boy first night is blocked consistently with the required-action set. |
| Guard | `game_play.php`, `game_functions.php` | `src/game.ts` `castGuard`, guarded night resolution; `src/room-client.ts` prompt/reminder gating | `tests/game.test.ts` guard role, first-night rejection, self-protection rejection, and protected kill tests; `tests/room.test.ts` guard authorization/persistence, first-night rejection, and self-protection websocket rejection tests | Implemented | The reference shared night-vote validation rejects dead targets and self-targets; guard self-protection is rejected server-side. Consecutive target restrictions are not modeled unless a future PHP extraction shows them in this build. |
| Cat revive and cat survival | `game_play.php`, `game_functions.php`, `game_vote.php` | `src/game.ts` `castCatRevive`, `pickCatRevival`, cat attack survival, optional night-action completion; `src/room-client.ts` revive-target prompt and required-reminder split | `tests/game.test.ts` cat option, first-night rejection, revive/survival/poison tests, optional revive completion, and no sudden-death for skipped revive; `tests/room.test.ts` cat action authorization/persistence and first-night rejection tests; `tests/render.test.ts` optional cat prompt versus required reminder hooks | Implemented | Random odds are pinned with deterministic random values; live prompts require an actual eligible dead target. Reference `CAT_DO` can be submitted but is not counted in the required night-action total, so skipped revive does not block dawn or trigger sudden death. |
| Poison and linked deaths | `game_functions.php` victory/death helpers | `src/game.ts` `resolveDay`, `resolveNight`, `applyLinkedDeaths` | `tests/game.test.ts` poison execution, poison night kill, cat-as-poison, lover/betrayer linked-death tests | Implemented | Exact random target selection is deterministic in tests. |
| Win conditions: villagers, wolves, foxes, lovers, draw | `game_functions.php` victory helpers | `src/game.ts` `determineWinner`, `withWinOrNextDay`, `resolveDay` draw handling; `src/render.ts` draw winner labels/icons in records/transcripts | `tests/game.test.ts` wolf win, fox win, child-fox fox win including wolf-extinction priority, big-wolf + child-fox wolf/fox priority tests, lovers cross-faction four-player threshold, same-faction lover non-win, more-than-four lover non-win, one-lover non-win, big-wolf counting tests, revote draw tests, and all-dead sudden-death draw tests; `tests/render.test.ts` draw record/transcript labels | Implemented | Lovers follow the reference `total_count <= 4`, exactly two living lovers, different main-faction rule. Child foxes count as foxes for wolf-extinction victory, while big-wolf + child-fox rooms follow the reference wolf-win branch where a lone child fox does not steal wolf victory unless enough real fox/big-wolf presence remains. Draw is represented as a first-class winner for repeated tied votes, all-dead sudden death, GM declaration, and old-log/game-record rendering. Additional rare PHP edge cases can be added as discovered. |
| Last words | `game_play.php`, `game_functions.php` | `src/game.ts` `setLastWords`, death logs; `src/room.ts` websocket handler | `tests/game.test.ts` last words after execution/night/fox death; `tests/room.test.ts` websocket last-words tests | Implemented | UI presentation is current-port style, not exact PHP layout. |
| Objection | `game_play.php`, SWF notification paths, `ROOMEND` protest vote | `src/game.ts` `raiseObjection` and `requestRoomEnd`; `src/messages.ts` public objection counts and same-day room-end voter filtering; `src/index.ts` safe legacy `game_play.php?set_objection=` redirects; `src/render.ts` reference objection image button, legacy `廢` control, and transcript labels for room-end requests; `src/room-client.ts` remaining-count controls, room-end command, and browser beep | `tests/game.test.ts` objection limits and room-end majority threshold; `tests/room.test.ts` objection persistence and room-end websocket finalization; `tests/messages.test.ts` objection count and same-day room-end voter filtering; `tests/validation.test.ts` `room_end_vote`; `tests/index.test.ts` legacy `set_objection=set` / `roomend` redirect tests; `tests/render.test.ts` reference image/count, room-end command, room-end transcript label, and sound controls | Implemented | Command, remaining-count UI, reference image chrome, browser beep, reference-style same-day majority room-end request, legacy `set_objection` link fallback, and old-log transcript labeling are covered. Legacy SWF playback is intentionally replaced by browser audio, matching the port strategy used for other sound notifications; legacy GET links redirect safely while DO/WebSocket remains authoritative for mutations. |
| Private channels: wolf, fox, common, lovers, dead, self-talk | `game_play.php`, `game_functions.php` talk locations | `src/game.ts` channel guards; `src/room.ts` filtered broadcasts, PHP-style composite wolf/fox+lovers live delivery, `chdis` fallback routing, and transcript persistence | `tests/game.test.ts` channel permission tests; `tests/room.test.ts` private broadcast, common voice, composite lover wolf/fox broadcast, restricted-channel fallback, dead chat, self-talk tests | Implemented | Live wolf/fox messages from lover players also reach lover sockets and persist `night wolf lovers` / `night fox lovers` locations like the reference. GM `chdis` restrictions route blocked wolf/fox/common/lovers talk to `night lovers` or `night self_talk` instead of dropping valid role speech. Viewer-specific old-log masking remains partial. |
| GM controls and whispers | `game_play.php`, `game_vote.php` `GMVote` / `GM_CHANNEL`, admin/GM paths | `src/room.ts`, `src/game.ts` force controls, common-voice toggle, GM-kill vote/action reset, life/role-change pending-action cleanup, target-name persistence for GM operation logs, and `chdis` channel restriction persistence; `src/index.ts` room-summary parsing, room-admin status filters, runtime-config admin writes, admin room-ended audit rows, admin menu routing, admin status aliases, `/api/protocol` GM command metadata, and legacy `adpass`/token room-admin compatibility including bare `admin.php` login entry and reference-style room-end redirects; `src/render.ts` admin menu/room/config/status context plus PHP-style transcript speaker labels for `自言自語` and `GM → 目標`, GM-only live control-row visibility; `src/room-client.ts` GM controls, live GM action panel, and GM/non-GM body mode toggles | `tests/game.test.ts` force-end/role/life/pending-action cleanup/GM-kill reset/flag/channel restriction tests; `tests/room.test.ts` GM websocket, whisper, target-name persistence, GM-kill reset, life/role-change pending-action cleanup, common-voice, and channel-restriction tests; `tests/index.test.ts` room option/admin parsing, active/ended/all filters, admin menu, legacy admin login/logout cookies, room-admin cookie authorization, bare `admin.php` cookie/token room-admin entry, room-admin end-link redirect, config admin update, `/api/protocol` GM command list, and `/admin/status` / `admin.php?go=status` tests; `tests/render.test.ts` room-list/admin/config/status markers, live GM action panel visibility markers, and transcript self-talk/GM-whisper/GM-target-name/admin-room-ended labels | Partial | Core controls, common voice, room summaries, filtered admin waste-room context, runtime config administration, admin tool navigation including modern and PHP-style status checks, legacy room-admin `adpass`/token compatibility including bare `admin.php` login entry and reference-style post-end room-list redirects, live GM action panel/status, non-GM presentation hides GM-only control rows until websocket membership marks the viewer as GM, GM kill resets pending votes/actions like the reference `GM_KILL`, role changes clear pending actions involving the adjusted player, GM life/role/flag operation logs persist the target nickname for old-log display, admin room-ended events render localized old-log status, `/api/protocol` lists the GM common-voice and channel-restriction commands exposed by the room websocket, legacy-style `chdis:ch_wolf:ch_common:ch_lovers:ch_fox` restriction matrix, and PHP-shaped self-talk / GM-whisper transcript labels exist; broader server-management screens and exact authenticated GM presentation remain partial. |
| Timed phases, silence, sudden death | `game_play.php` flow checks | `src/game.ts` alarms, silence acceleration, warning/death handling; `src/messages.ts` warning timestamp; `src/room-client.ts` phase/revote/warning beep | `tests/game.test.ts` timer defaults, silence acceleration, realtime no-acceleration, sudden-death warnings; `tests/room.test.ts` DO alarm tests; `tests/messages.test.ts` warning timestamp; `tests/render.test.ts` state sound hooks | Implemented | Legacy SWF/cookie playback remains intentionally not exact. |
| Lobby start, leave, kick votes | `user_manager.php`, `game_vote.php`, `room_manager.php` resident handling | `src/game.ts` lobby vote/leave/kick helpers, `src/index.ts` legacy `room_manager.php` create redirect, `game_play.php?go=out` logout alias, and safe legacy `user_manager.php` registration POST redirect with normalized `auto_reload` preservation, reference 8-player resident-start minimum and dummy-boy start-vote credit; `src/messages.ts` filtered lobby kick-vote status; `src/room.ts` websocket commands and reference-style leave/kick reset logs; `src/render.ts` transcript vote-count/kick-method payload labels plus the `user_manager.php` resident-registration form shell with reference field names; `src/room-client.ts` lobby kick-vote target status | `tests/game.test.ts` lobby host/start/kick/leave, `tests/index.test.ts` legacy room-manager create redirect, legacy logout alias, legacy user-manager POST alias, start-vote minimum, dummy-boy start-vote credit, and `auto_reload` redirect preservation tests; `tests/messages.test.ts` filtered lobby kick-vote status; `tests/room.test.ts` websocket lobby start/kick/leave, direct kick reset-log, vote-kick reset-log, lobby leave reset-log, and dummy-boy resident start-vote tests; `tests/render.test.ts` lobby kick-vote status rendering hooks, resident-registration legacy form checks, and transcript vote-count/kick-method detail tests | Partial | Current implementation covers resident start/kick/leave plus the PHP-style `room_manager.php` create-to-login redirect, `game_play.php?go=out` logout redirect, and `user_manager.php` registration POST compatibility redirect with normalized `auto_reload`, while actual joining remains DO/WebSocket-authorized; it also covers the reference start-vote minimum, dummy-boy vote credit, filtered live kick-vote target status, leave/kick success vote reset/logging for direct leave, direct kick, and resident-vote kick paths, plus transcript vote-count/kick-method details; deeper exact authenticated lobby presentation remains partial. |
| D1 records and player stats | `game_functions.php` old log/stat paths | `src/room.ts` `syncRoomStatus`, `src/index.ts` record/stat routes; `src/render.ts` localized finalized `game_ended` transcript rows | `tests/room.test.ts` final record/stat sync; `tests/index.test.ts` records/profile/leaderboard tests; `tests/render.test.ts` finalized game-ended transcript labels | Implemented | Database writes are batched at game end as required by the Cloudflare port constraints, and the finalized room event renders localized winner/day/player-count labels in old-log transcripts. |
| Betrayer/cult asset naming | `setting.php`, `index.php`, `game_functions.php`, `game_vote.php`, `lang/jpn/rule.php` | `src/types.ts` `betrayer`; `src/game.ts` `applyRoomOptions`, linked deaths; `src/render.ts`, `src/room-client.ts` role icon/label | `tests/game.test.ts` betrayer option and linked-death tests; `tests/render.test.ts` betrayer label/icon tests; `docs/reference-betrayer-cult-extraction.md` | Implemented | Reference `role_cult.gif`/`role_cult_partner.gif` are assigned to `$role_betr_image`; no separate cult rule path was found in this build. |
| Viewer-specific transcript masking | `game_functions.php` talk output branches | `src/index.ts` private-event filtering and explicit transcript viewer query validation; `src/render.ts` transcript grouping/styling plus `viewer=public/player/dead/gm` masks, localized event-history labels, saved PHP-style talk-location labels, room-created capacity labels, join/login event labels, localized phase/winner payload labels, player-facing ability/medium result labels, legacy system action payload labels, and GM operation payload details, record/event-derived player-view selectors with saved/event nickname labels, payload `playerId`/`actorPlayerId` ownership fallback, record-or-event-derived role fallback for player channel visibility including PHP `lovers` and `common` compound role strings, PHP-style composite wolf/fox+lovers row visibility, state-preserving old-log display links, same-alias PHP display/view navigation, PHP-style public/dead/GM and player-view old-log/game-log links/forms, GM channel-control system rows, reference-like transcript-table/day-heading/column/speaker-marker chrome, and explicit-system event classification for private rows without player ids; `src/room-client.ts` localized live event-panel labels | `tests/index.test.ts` private event hidden until ended and transcript viewer query tests, including required and room-history-known `viewer_player_id`, direct `old_log.php` player-view alias masking/hidden-field/link preservation, player-view links, active player-view labels, PHP-style player-view form actions, PHP alias display/view link preservation, and teammate channel masking; `tests/render.test.ts` event-history label tests, live event-panel label script tests, transcript location styling and transcript-table/day-heading/column/speaker-marker chrome, viewer-mode filtering, room-created capacity labels, join-event labels, no-player-id private-row masking, player selector rendering, payload actor id ownership and selector fallback without public leakage, active player-view labels, legacy alias player-view form/rendered navigation rendering, role-derived teammate channel visibility, event-payload role fallback channel visibility, PHP-role-string lover/common fallback visibility from events and records, real-fox-only fox-chat visibility, composite wolf/fox lovers visibility and labels, saved talk-location label tests, saved PHP `beforegame/day/night system` location labels, saved PHP system payload code labels, saved ability/medium result label tests, saved GM operation detail tests, localized phase/draw payload tests, state-preserving transcript links, PHP-style public/dead/GM and player-view alias links, and heaven-only GM operation visibility | Partial | Additive viewer modes now cover public, own-player private rows including role-action rows whose actor is stored only in payload `playerId`/`actorPlayerId`, same-team wolf/common/lovers rows including saved PHP `common *` and `* lovers` role strings from records or events, real-fox-only fox chat rows derived from saved records or saved event payload roles, PHP-style `night wolf lovers` / `night fox lovers` composite rows and labels for lover player views, localized live event-panel and event-history labels, room-created capacity and join/login system rows, localized phase/draw transcript payloads, saved ability and medium result labels, saved PHP `beforegame system` / `day system` / `night system` labels, legacy `KICK_DO` / `FKICK_DO` / `VOTE_DO` / `WOLF_EAT` / `MAGE_DO` / `FOSI_DO` / `CAT_DO` / `GUARD_DO` / `OBJECTION` / `ROOMEND` payload labels, saved GM life/flag/common-voice/channel-restriction operation details, addressed GM whispers, dead/heaven, GM transcript perspectives, nickname-based active player-view labels, legacy alias-preserving player-view selection forms, same-alias old-log/game-log display/view quick links, and a reference-like transcript table/day heading/column/speaker-marker layout; player mode requires an explicit viewer id that appears in saved records or event actor/target payloads, private rows without player ids are not treated as system-visible rows, and the page exposes direct modern plus PHP-style public/dead/GM/player-view choices, but exact authenticated identity semantics remain partial. |
| Visual/browser parity | `game_view.php`, `game_play.php`, `game_frame.php`, `game_up.php`, `index.php` | `src/index.ts`, `src/render.ts`, `src/room-client.ts`, `docs/visual-parity-checklist.md` | `scripts/smoke-local-ui.mjs`, `tests/local-ui-smoke.test.mjs` core page plus legacy frame/up/bottom/vote HTTP shape checks; `tests/render.test.ts` room view-mode visibility checks, legacy frameset shell checks, bottom-frame mode checks, compact `game_up.php` upper-frame CSS/link checks, bottom-frame duplicate-chat-control and diagnostic/page-explanation hiding checks, `game_vote.php` header-chrome/player-grid/entry-map/page-explanation/duplicate-action-panel hiding checks, and `game_up.php` send-form shell checks; `tests/index.test.ts` legacy `game_frame.php`, `game_play.php?frame=bottom`, safe `game_play.php` talk POST redirect, bottom-frame duplicate-chat-control and diagnostic/page-explanation hiding checks, `game_vote.php` header-chrome/player-grid/entry-map/page-explanation/duplicate-action-panel hiding checks, and compact `game_up.php` send-form/link response checks | Partial | Manual/browser capture matrix exists; room player grids are visible across player/spectator/heaven views while resident-only registration and ability-operation rows stay player-only, spectator view hides chat controls, heaven view hides live-player chat controls while keeping heaven chat entry visible, local HTTP UI smoke now also checks `game_frame.php`, `game_up.php`, `game_play.php?frame=bottom`, and `game_vote.php` rendered entry shapes, `game_frame.php` now renders a real frameset shell, `game_play.php?frame=bottom` hides registration/PHP-entry/diagnostic/page-explanation chrome and duplicate chat controls while keeping the main game panels and chat log, traditional `game_play.php` talk POSTs redirect safely back to the full or bottom panel while preserving normalized `auto_reload`, `game_vote.php` hides header full-room chrome, the general player grid, the diagnostic entry-map/page-explanation chrome, and the duplicate normal action panel while keeping the PHP-shaped vote form, and `game_up.php` emits a compact reference-shaped `form name="send"` targeting the bottom frame with the normal room header hidden and a bottom-targeted vote/action link; no browser or screenshot baseline is available in this environment. |

## Verification Gates

Current parity work should continue to pass:

- `npm test`
- `npm run typecheck`
- `npm run check:wrangler`
- `npm run check:whitespace`
- `npm run check:d1-schema`

## Next Rule Work

1. Add focused tests for any newly discovered victory edge cases from `game_functions.php`.
2. Decide whether viewer-specific old-log masking should be implemented as separate endpoints/modes or as parameters on `/room/:roomId/log`.
3. Run `docs/visual-parity-checklist.md` and add screenshot-based UI parity once a browser is available.
