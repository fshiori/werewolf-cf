# Reference Port Gap Audit

Date: 2026-05-07

Reference target: `ref/diam1.3.61.kz_Build0912`

Scope: UI/UX and game-rule parity audit for the Cloudflare Workers/D1/Durable Objects port. This file is a tracking artifact; it does not change or copy files from `ref/`.

## Current Evidence

Implemented Cloudflare artifacts:

- Worker routes: `src/index.ts`
- Rendered pages: `src/render.ts`
- Room browser client: `src/room-client.ts`
- Durable Object room authority and WebSocket filtering: `src/room.ts`
- Core game state machine: `src/game.ts`
- D1 schema: `migrations/`
- Local rendered UI smoke: `scripts/smoke-local-ui.mjs`
- Reference asset inventory: `docs/reference-asset-inventory.md`, `scripts/inventory-reference-assets.mjs`
- Rule parity manifest: `docs/rule-parity-manifest.md`
- Visual parity checklist: `docs/visual-parity-checklist.md`
- Automated tests: `tests/`

Reference files inspected:

- `index.php`: top page, menu, room creation form, announcement, room list include.
- `list.php`: federated room list layout.
- `game_view.php`: spectator room layout, player list, logs, voting/death/last-words sections, phase color handling.
- `game_play.php`: logged-in player room layout, talk/action flow, death/heaven mode, exit handling.
- `game_functions.php`: output helpers for talk logs, victory, dead players, ability actions, votes, players, last words, revote, victory rules.
- `user_manager.php`: resident registration, icon/trip/wish role handling.
- `room_manager.php`: room creation and room list behavior.

## Prompt-To-Artifact Checklist

| Requirement | Current artifact/evidence | Status |
| --- | --- | --- |
| Cloudflare Workers runtime | `src/index.ts`, `wrangler.toml`, `npm run check:wrangler` | Implemented |
| D1 persistence | `migrations/`, including `trip_scores` read-model storage, `RoomDurableObject.syncRoomStatus`, `tests/d1-schema.test.mjs` | Implemented |
| Durable Objects + WebSockets source of truth | `src/room.ts`, `tests/room.test.ts` | Implemented |
| DO alarms for day/night | `RoomDurableObject.alarm`, `advancePhaseByAlarm`, alarm tests | Implemented |
| R2 avatar storage | `/api/assets/avatar`, `/assets/avatar/:playerId`, write smoke | Implemented |
| KV runtime config | `/api/config`, `/announcement.txt`, `/status`, `getHomeAnnouncement`, `isMaintenanceMode`; `/status` renders reference-style health markers for overall, maintenance, and binding states; `/announcement.txt` exposes the home announcement as legacy plain text | Implemented |
| Rendered non-JSON pages | `/`, `/announcement.txt`, `/list`, `/logs`, `/bbs`, `/admin`, `/admin/bbs`, `/admin/rooms`, `/admin/config`, `/trip`, `/trips`, `/room/:id`, `/leaderboard`, `/stats`, `/icons`, `/status`, `/room/:id/records`, `/room/:id/events`, `/player/:id`, `/rules`, `/manual`, `/script-info`, `/protocol`, `/version`; PHP-style aliases `/index.php`, GET `/room_manager.php`, `/list.php`, `/old_log.php`, `/bbs.php`, `/icon_view.php`, `/icon_upload.php`, `/upload.php`, `/upload2.php`, `/rule.php`, `/lang/jpn/rule.php`, `/script_info.php`, `/lang/jpn/script_info.php`, `/stats.php`, `/trip.php`, `/trip.php?go=icon`, `/trip.php?go=smess&id=:trip`, `/trip.php?go=sce&room=:id&trip=:trip`, `/version.php`, `/lang/cht/version.htm`, `/admin.php`, `/admin.php?go=rooms`, `/admin.php?go=config`, `/admin.php?go=bbs`, `/admin.php?go=del&id=:id`, `/game_log.php?room_no=:id`, `/game_view.php?room_no=:id`, `/game_play.php?room_no=:id`, `/game_play.php?go=del&id=:id&room_no=:id`, `/game_frame.php?room_no=:id`, `/game_up.php?room_no=:id`, GET `/game_vote.php?room_no=:id`, `/login.php?room_no=:id`, `/user_manager.php?room_no=:id` | Implemented |
| Room page client script not inline | `/assets/room-client.js`, `src/room-client.ts`, local UI smoke | Implemented |
| Reference top/menu visual style | Table layout, side menu, fieldsets, colors, reference `<body bgcolor="white">`, reference top title/background asset URLs, reference-shaped home/list/old-log/icon/version/rule/script-info page titles, rule/script-info page return/title-image chrome, PHP-style home room-entry links, reference-style noscript warning, current-page bold menu markers on major legacy pages, reference-style footer credits, and create-room option bitmap labels in `src/render.ts` | Partial |
| Reference room player grid visual style | `renderRoom`, `.player-grid`, `.player-card`, role/death/vote styling including the reference 800px/10pt/dotted player-list table chrome, reference pink lobby-start-voted highlighting, and the reference pregame start-vote notice; client emits reference role icons and colored role text for revealed roles plus grave fallback for dead players, shows the current player's role with the matching reference role icon, and role partner websocket lists exclude the viewer like the reference `uname <> self` partner queries; PHP-style live room/login/resident aliases route into the same room renderer with spectator/player view defaults, and room headers expose PHP-style `game_play.php`/`game_view.php`/heaven/`game_frame.php`/`game_up.php`/`game_vote.php`/login/resident links that preserve `auto_reload`; the resident registration surface emits reference title/name/Trip/role/icon label assets and wish-role bitmap choices with text fallback | Partial |
| Reference chat/log/death/vote sections | Room page has chat/game log/records/events, reference-style live phase labels with day number and survivor count, sudden-death day/night hurry-up banners, live action prompts for day voting and night role targets plus the dead-player ability prompt, first-night non-wolf role-action prompt suppression, a live public last-words panel with reference heading/row chrome, reference-style red unvoted reminders for day votes and eligible night role actions, viewer-specific selected night-action target prompts, live chat messages rendered as reference-like speaker/message table rows, and live game-log system rows rendered as reference-like table rows with grey/vote/danger/death coloring and reference MORNING/NIGHT display text; public/private channel/action events, including room-created rows with capacity, join/login rows, objection notifications, same-day majority room-end request events, player leave events, admin room-ended rows, and finalized game-ended rows, persist to `room_events`; live room event panels, rendered event-history pages, and transcript pages use localized event labels; private transcript entries are hidden from `/events` and `/log` until the room status is ended | Partial |
| Reference icons and bitmap assets | R2 upload exists; `docs/reference-asset-inventory.md` classifies 130 reference assets; `/assets/reference/:path` serves vetted copied R2 assets; top chrome, home/admin/federated room-list status/options, create-room option labels, old-log title/background/capacity/discrete reference option/victory rows, icon catalog/upload title/background chrome, rule/script-info title/background chrome, resident registration title/field labels and wish-role choices, rules role rows, live/profile/transcript/room-record/Trip-record role markers, and game-record/Trip-record victory rows emit reference image URLs with text fallback | Partial |
| Browser E2E/manual visual verification | Local HTTP UI smoke plus `docs/visual-parity-checklist.md` capture plan; no installed browser detected | Partial |
| Screenshot/visual parity against ref | `docs/visual-parity-checklist.md` defines the capture matrix and evidence paths; no screenshot baseline has been generated | Missing |
| Complete PHP rule parity | `docs/rule-parity-manifest.md` maps PHP rule areas to TypeScript artifacts and focused tests; core roles/options implemented; focused tests cover reference-style lover-only threshold/faction wins, same-faction and more-than-four lover non-wins, one-lover non-wins, big-wolf win counting, big-wolf + child-fox wolf/fox priority, child-fox fox wins, reference medium result categories for big wolf/child fox, multiple same-day medium readings from day sudden deaths plus execution, repeated-tie draw, all-dead sudden-death draw, vote visibility, non-realtime silence acceleration, timed sudden-death warning windows, and timed sudden-death alarm handling | Partial |
| Federated room list (`list.php`) | `/list` renders a reference-style federated list from local D1 rooms and optional remote peers configured by KV `federated_servers`, including a reference-shaped `汝等是人是狼？ - ...` page title, reference status icons, PHP-style local `login.php?room_no=:id` room links, main-table peer service rows with `服務中`/`失聯中` and URL-only host/path links like the reference, a five-column legacy room table shape with reference widths, optional unwrapped `back_page` return links before the list fieldset, and peer health markers; configured peer names are retained in the diagnostic status table, configured peer status is shown including failed peers, and configured peer base paths are preserved like the reference host/path pairs; `/api.php` emits a reference-style tab-separated active-room feed for legacy federated consumers, and configured peers can fall back from JSON `/api/rooms` to legacy `/api.php` TSV feeds | Partial |
| Discussion board (`bbs.php`) | `/bbs` and `/bbs.php` render a reference-style topic list with the legacy `$lang_title - $server_comment` page title, pinned/locked/digest status markers, legacy unwrapped mode-specific `bbs.php?go=post`/`bbs.php?go=dige`/`bbs.php` top links, digest-topic list linked from the side menu, paginated topic lists, topic titles linked through `bbs.php?view=:id` to the latest reply page, topic list table chrome matching reference `table1`/`table3` classes, widths, `No.`/`最後時間` headers, and `沒有主題`/`沒有精華` empty rows, topic detail views via `/bbs.php?view=:id`, legacy `/bbs.php?go=postre&id=:id`, legacy `/bbs.php?go=edit&id=:id`, and `/bbs/:id` with the same legacy page title, reference unwrapped `回覆主題`/`回列表` top links, reference `文章列表`, `#table5`, `table1`/`table2`/`table3`/`table4` post/reply chrome, safe reference-style `[url]`/`[color]`/`[b]` BBCode rendering, newline `<br />` rendering, and `NO.id <..> [time]` footer rows, paginated replies, post form, reply form, locked-topic no-reply notice, and topic moderation panel backed by D1 `bbs_topics`/`bbs_replies`; topic/reply forms accept optional edit/delete passwords and store hashes in D1; topic and reply content can be edited by either BBS admin token or matching post password; topics and replies can be deleted by either BBS admin token or matching post password while maintaining reply counts; JSON topic creation returns the D1 topic id, and legacy `POST /bbs.php?go=post` accepts reference `bname`/`bpass`/`title`/`mess` fields before redirecting to `bbs.php?view=:id`; the rendered topic list also exposes a reference-field legacy `form name="bbs"` post form with reference-shaped text/password sizes and submit id for the same handler; JSON reply creation returns the target page, and legacy `POST /bbs.php?go=postre` accepts reference `id`/`bname`/`bpass`/`mess` fields before redirecting to the paginated `bbs.php?view=:id` topic page; the rendered topic detail also exposes reference-field legacy `form name="bbs"` reply/edit forms, reference-shaped reply input sizes and submit id, and PHP-style list links for the same handler; legacy `POST /bbs.php?go=edit&id=:id` accepts reference `editis`/`password` form operations for topic edit/delete, reply edit/delete, and admin-only topic lock/pin/digest toggles before redirecting through `bbs.php?view=:id`; the rendered moderation panel exposes the same legacy edit/delete/lock/pin/digest operation selector plus reference-shaped password value and submit id; `/admin/bbs` lists paginated topics with the same status markers and links to token-protected moderation controls that can pin, lock, mark digest, edit topic/reply content, delete topics with their replies, and delete individual replies while maintaining reply counts | Partial |
| Icon catalog/upload parity | `/icons` renders the reference default icon catalog with icon-view/upload page titles, title image chrome followed by `<br>`, background image chrome, PHP-style `icon_view.php`/`icon_upload.php` crosslinks, upload aliases with the upload menu item marked active and the reference `用戶圖像上傳` title, reference-style unwrapped `←返回` links followed by `<br>`, reference-style five-icon wrapping catalog table with bordered icons and color metadata, local default-icon picker controls, R2 avatar upload/remove controls, direct legacy `upload.php`/`upload2.php` forms, reference-style upload warning copy, reference-shaped `icon_file`/`icon_name` field chrome and `登錄` submit label, the reference custom `color` field, and the full 216-entry reference color swatch table; room join can send a vetted default `iconPath`; avatar upload exists on room and icon pages; legacy GET `/upload.php` and `/upload2.php` render the same upload/catalog surface, while POST aliases accept reference-style form field names, reuse the validated R2 avatar handlers, and return reference-shaped HTML result pages including the `upload2.php` one-second deletion redirect instead of JSON API responses | Partial |
| Old logs (`old_log.php`) | `/logs` and `/old_log.php` render a reference-style ended-room index with legacy `old_log.php?all=1`, `old_log.php?search=:term` search filtering, reference page title, reference unwrapped `index.php` return/all links followed by `<br><br>`, reference title image followed by `<br>`, 25-row pagination with search-preserving page links, latest game-record victory icons, and `old_log.php?log_mode=on&room_no=:id` normal/reverse/heaven links plus max-player icons and the reference twelve fixed option columns; old-log detail links preserve the active index `search`, `page`, or `all` state and transcript/record/event pages use that state in the reference-style `←返回` link; `/room/:roomId/records`, `/room/:roomId/log`, `/old_log.php?log_mode=on&room_no=:id`, and `/game_log.php?room_no=:id` render D1 game records with role-icon player result rows and localized victory/draw labels; room record/event/transcript headers link back through `game_view.php?room_no=:id`, record/event headers link both `old_log.php?log_mode=on&room_no=:id` and `game_log.php?room_no=:id&log_mode=on`, and room record/event/transcript pages include the reference old-log title image/background chrome plus old-log return links; `/room/:roomId/log` and the PHP-style `old_log.php`/`game_log.php` aliases also render full room event history instead of the recent 50-event API window, day/phase transcript sections, localized phase/winner payload labels, historical vote tables grouped by day/revote round with aggregate and per-ballot target counts, PHP-style `N 日目 ( M 回目)` vote headings, persisted vote-round payload labels, bordered zero-spacing vote table attributes, bold voter/target cells, reference-inspired vote-table coloring, location labels/colors for public/private/system/action rows, room-end request labels, `heaven_talk`/`heaven_only`/`reverse_log` display modes, state-preserving `old_log.php`/`game_log.php?log_mode=on` header aliases, same-alias display/view quick links when entered through `old_log.php` or `game_log.php`, a reference-style old-log return link, and additive `viewer=public/player/dead/gm` masking modes with visible scope labels, PHP-style public/dead/GM and player-view links/selectors, own private rows matched from event `playerId` plus payload `playerId`/`actorPlayerId`, addressed GM whispers, saved-role-derived same-team wolf/fox/common/lovers channel rows, and no-player-id private row masking so imported/private rows are not treated as public system entries; private entries are visible only after the room ends | Partial |
| Trip identity parity | `/trip` and `/trip.php` provide reference-style identity registration, claim, exclusion, lookup, `修改Trip`, `修改紀錄`, and legacy account-claim compatibility controls with status markers, the legacy `$lang_title - $server_comment` page title, legacy subnavigation, plus legacy `form name="trip"` actions for Trip registration and record exclusion with reference-shaped text/password sizes, empty values, and submit ids; `/trips` and `/api/trips/lookup` provide public lookup with registered/excluded status markers, claimed players, aggregate stats, D1-backed Trip score counts, the same legacy page title, and the reference-style `trip.php?go=search&sname=` GET search form without exposing stored Trip hashes; legacy `trip.php?go=trip&id=:trip` renders a server-side public Trip detail table without exposing stored hashes and links to the matching `trip.php?go=room&id=:trip` participation table plus score-count/comment links; legacy `trip.php?go=search&sname=:trip` redirects valid exact searches to that Trip detail and empty searches render the lookup page; legacy `trip.php?go=smess&id=:trip` renders D1-backed `trip_scores` comment rows with reference-shaped room, reviewer, score, and message columns plus the no-data fallback; legacy `trip.php?go=sce&room=:id&trip=:trip` renders the reference-shaped Trip rating form as disabled compatibility chrome and direct legacy score POSTs return reference-shaped 501 result pages because the PHP session-authenticated score write path is not yet represented in the Cloudflare identity model; legacy `trip.php?go=room&id=:trip` renders server-side participation records from claimed player game records without exposing stored hashes, exposes the reference `全部/8/16/22/30` capacity filter links, applies valid `play=8/16/22/30` filters against room capacity, paginates at the reference 15-row page size while preserving `play=`, emits reference victory icons, and links room ids to `old_log.php?log_mode=on&room_no=:id` like the reference table; legacy `trip.php?go=icon` opens the shared reference-style icon catalog/upload flow; legacy `POST /trip.php?go=post` accepts reference `name`/`password` fields for Trip registration and legacy `POST /trip.php?go=out` accepts reference `name`/`password`/`aname` fields for Trip exclusion before redirecting to `/trip.php`; Trip edit/migration/account-claim forms are rendered as disabled compatibility surfaces with reference-shaped empty field values, and direct legacy `POST /trip.php?go=edit` / `go=edit2` / `go=accadd` return reference-shaped 501 result pages because the PHP-era password model is not stored in the Cloudflare schema | Partial |

## UI Parity Findings

### Implemented or close

- Top page uses a reference-like table shell, side menu, fieldsets, room list, announcement, and create-room form.
- Room creation covers max users, comments, real-time timing, wish role, Trip-required, GM, dummy boy, last words, open vote, common voice, dead role visibility, self-vote, vote-status, and major optional roles; the room join selector and assignment path now support enabled optional-role wishes with PHP-style randomized conflict ordering. `docs/reference-wish-role-extraction.md` records the implemented ordering behavior and the intentional rejection of the PHP loose-comparison quirk.
- Room page uses retro table panels for header, player list, actions/votes, public last words, chat, logs, records, and events.
- Core game-loop tests now cover an opening day, execution, seer night action, wolf night kill, medium result delivery on the next day, second-day wolf execution, and village victory as one continuous lifecycle.
- Room page now loads client logic through `/assets/room-client.js` instead of embedding the whole WebSocket client in the HTML.
- `/admin` now provides a reference-style management menu linking room administration, BBS topic management, runtime configuration, and status checks while leaving each admin write tool token-protected; rendered admin subtool links use PHP-style `admin.php?go=rooms`, `admin.php?go=config`, `admin.php?go=bbs`, and `admin.php?go=status` aliases while the modern `/admin/...` URLs remain accepted. It also exposes a legacy `admin.php?go=in` form using the reference `apass` field, keeps accepting the older `adpass` alias, writes/accepts a legacy-style `adpass` cookie for room administration while preserving token query compatibility, redirects login to bare `admin.php` with the token query, renders the reference-style room-admin list from bare `admin.php` when that cookie or a token query is present, and redirects legacy logout `admin.php?go=out` back to `index.php` while clearing that cookie like the reference. `/admin/rooms` and `admin.php?go=rooms` provide a KV-token-protected reference-style `廢村管理` list with active/ended/all filters, room comments, capacity, option context, `game_view.php?room_no=:id` room links, `game_log.php?room_no=:id&log_mode=on` log links, modern diagnostic event links, ended-room inspection, direct legacy `admin.php?go=del&id=:id` end links, and D1-backed room-ending action for active rooms; legacy `admin.php?go=del&id=:id` and `game_play.php?go=del&id=:id&room_no=:id` links reuse the same room-admin token or legacy `adpass` cookie and end-room mutation before redirecting back to bare `admin.php`/room administration; `/admin/config` and `admin.php?go=config` provide token-protected KV announcement and maintenance-mode controls; `/admin/status`, `/status`, and `admin.php?go=status` provide read-only health/runtime status; `/admin/bbs` and `admin.php?go=bbs` provide BBS topic administration.

### Still missing or weak

- Reference uses bitmap title/background/role/status/option/victory images such as `img/top_title.jpg`, `img/top_bg.jpg`, `img/playing.gif`, `img/waiting.gif`, role images, option icons, and victory result images. Current top chrome, home/federated/old-log room-list status/capacity/option rows, rules role rows, old-log and player-profile result rows, revealed live-room role markers, dead fallback, and game-record victory rows now emit copied R2 asset URLs while keeping text fallback.
- Reference menu includes `聯合列表`, script info, old logs, icon view/upload, win-rate analysis, BBS, digest articles, Trip registration, and manual/help-style navigation. Current menu, script-info related-page links, manual/help navigation, federated list, admin entry, and home room rows have the core app pages using PHP-style aliases for the matching legacy page filenames, `admin.php` management entry, and `login.php?room_no=:id` active-room entry links, a configurable federated-list page, an old-log index/detail flow, script info, win-rate analysis, separate icon catalog/upload menu links backed by the combined icon page, Trip registration/lookup, BBS topic/reply and digest views, a dedicated manual page, and a management-menu entry for the token-protected room/config/BBS admin tools, but not every legacy management page.
- Reference room view has phase-specific body colors, manual/auto refresh links, login/resident registration links, public player lists, and different layouts for spectator/player/heaven modes. Current room page now applies reference-style lobby/day/night/ended body colors as realtime state changes, includes manual/auto refresh controls for auxiliary panels, exposes PHP-style page reload links with 15/20/30-second meta refresh options, shows reference-style resident/Trip registration and icon-catalog links, keeps the public player grid visible across player/spectator/heaven views while hiding resident-only registration and ability-operation rows outside player view, hides chat controls for spectator view, keeps only heaven-appropriate chat controls in heaven view, has direct `view=player/spectator/heaven` page modes with preserved reload links and mode-specific player-only row visibility, and gives `game_frame.php`, `game_up.php`, and `game_vote.php` distinct server-rendered panel modes that hide auxiliary/live panels like their PHP-era entry intent, but still shares one realtime WebSocket layout rather than fully separate frame-era templates.
- Reference player list includes default icons, hover image swap, Trip links, role reveal text colors, already-voted background, and dead icon handling. Current player cards now emit selected default icons, reference role icons and colored text for revealed roles, profile links from nicknames, public Trip lookup links, already-voted backgrounds, a grave icon for dead players, and reference-style dead-icon hover swap back to the selected default icon, but still do not expose raw Trip hashes in live state.
- Reference talk log has many location-specific render paths: public day, night wolf, common, lovers, fox, self talk, heaven, GM broadcast/whisper, system action visibility, and post-game/dead visibility. Current WebSocket channels cover the major private channels plus night self-talk; live wolf/fox messages from lover players also reach lover sockets and persist PHP-style `night wolf lovers` / `night fox lovers` locations. GM `chdis` channel restrictions now route otherwise-valid wolf/fox/common/lovers speech to `night lovers` or `night self_talk` instead of rejecting it. Live chat rows now use reference-like speaker/message table cells with DOM text insertion for player text, and public/private transcript entries are persisted with room-status visibility filtering; `/room/:id/log` reads the full room event history, groups replay entries by day/phase, labels/styles major locations and role-action rows, renders saved room-created capacity and join/login events as system rows, renders admin room-ended rows with localized status payloads, renders finalized game-ended rows with localized winner/count payloads, renders saved ability and medium-result enums as player-facing result labels, localizes saved phase and draw winner payloads, uses saved PHP-style talk locations for composite wolf/fox+lovers, lovers fallback, common, self-talk, heaven, and `beforegame/day/night system` transcript labels, localizes legacy system action payloads such as `KICK_DO`, `FKICK_DO`, `VOTE_DO`, `WOLF_EAT`, `MAGE_DO`, `FOSI_DO`, `CAT_DO`, `GUARD_DO`, `OBJECTION`, and `ROOMEND`, includes PHP-shaped self-talk speaker labels and `GM → 目標` whisper labels, includes GM channel-control operations with saved target names plus life/flag/common-voice/channel-restriction details as system-visible transcript rows, includes lobby start/kick vote counts and kick methods in transcript payloads, supports old-log heaven/reverse display modes with state-preserving viewer links, and now has additive public/player/dead/GM viewer masks plus record/event-derived player-view selectors that include event actors, payload `playerId`/`actorPlayerId`, and event targets while labeling the active player view with saved/event nicknames. Legacy `old_log.php`/`game_log.php` aliases keep player-view selection forms on the same PHP-style alias with `room_no`/`log_mode` hidden fields. Player-view masks include the player’s own private rows even when imported role-action rows store ownership only in payload ids, GM whispers addressed to that player, record-derived or event-payload-role-derived same-team wolf/common/lovers rows including PHP `common *` and `* lovers` role strings from records or events, real-fox-only fox chat rows, and PHP-style `night wolf lovers` / `night fox lovers` composite rows for lover views; private rows without player ids are no longer classified as public system rows, but transcript parity still lacks exact PHP layout and broader authenticated historical identity semantics.
- Reference vote output renders per-day vote tables, open-vote visibility, revote messages, and dead/spectator differences. Current room UI shows summary/action state, marks day voters and required night action actors when vote-status is enabled without exposing hidden targets, labels live spectator/dead night progress as actions rather than day votes, prevents duplicate day votes until revote/reset clears the ballot map, sends each voter their own hidden vote target and own completed-vote state without broadcasting other hidden targets, sends night-action targets and own completed-action state only to the acting player's WebSocket even when vote-status is hidden, treats a single wolf-pack night kill as satisfying the reference wolf action count while still rejecting duplicate submissions from the same wolf, treats cat revive as an optional `CAT_DO` action that does not block dawn or sudden-death actors, displays the selected night target back to the actor, renders the live spectator/dead vote-status panel inside the public player-list panel so spectator/heaven layouts can actually see it, shows a red live revote action prompt, gives `game_vote.php` a PHP-shaped command/situation map for lobby kick/start, day execution votes, and night role actions plus a legacy-form shell with `command=vote`, situation options, state-synced target radio rows, hidden `situation`/`vote_times` fields and the disabled situation selector that follow the selected WebSocket command and live revote count, reference submit labels that submit the selected target through the existing WebSocket command path, back link, and dotted vote-list sample chrome, and old logs now render historical vote tables by day/revote round with aggregate totals, per-ballot target-count columns, persisted vote-round labels, PHP-shaped four-column `border=1 cellspacing=0 cellpadding=2 style="font-size:12pt;"` vote table chrome, and public-view non-open vote tables that preserve rows while hiding target-count text like PHP's `投票給→`; remaining live vote-status styling variants may still differ.

## Rule Parity Findings

### Implemented or close

- Role deck for 8 to 30 players exists in `REFERENCE_ROLE_DECKS`.
- Core phases: lobby, day, night, ended.
- Day vote, revote on tie, 10-tie draw, authority vote weight, decider tie handling.
- Night kill, seer, child fox, guard, optional cat revive.
- Poison/cat linked deaths, lover linked deaths, betrayer linked deaths.
- Winners: villagers, werewolves, foxes, lovers.
- Private channels: wolf, fox, common, lovers, dead, GM, GM whisper; GM can toggle common voice visibility and persist `chdis` restrictions for wolf/common/lovers/fox channels during active games, and room summaries render those restrictions as option markers.
- Dead role visibility and end-game role reveal.
- Last words and dummy boy first night, including forced dummy target and rejected/suppressed non-wolf role actions during that first night.
- D1 final records and player stats after game end.

### Still missing or weak

- `game_functions.php` has additional visibility branches for many system talk locations. Current implementation has equivalent permission checks for channels and now maps the main PHP system locations/action payloads in transcripts, but not full transcript layout/authenticated-view parity.
- `game_functions.php` victory logic includes special handling around lover-only victory and heavy wolf / fox edge cases. Current lover-only victory now follows the reference four-or-fewer living players, exactly two living lovers, and different main-faction condition; big-wolf + child-fox rooms now follow the reference wolf-win branch where a lone child fox does not steal wolf victory unless enough real fox/big-wolf presence remains. More reference scenarios may still need mapping as discovered.
- Reference `game_vote.php` assigns wishes by removing available wished roles from the current role list in randomized user order, then assigning leftovers. Current assignment now does this for core roles and enabled optional roles, including duplicate-wish conflict priority; the PHP loose-comparison index-0 quirk is intentionally not reproduced.
- Reference `cult` image filenames are used for the `betr` / `背德` role in this build; `docs/reference-betrayer-cult-extraction.md` maps that evidence to the implemented `betrayer` role and no separate cult rule path has been found.
- Reference has objection/sound/revote SWF paths, cookie-driven sound notifications, and same-day majority room-end requests. Current port implements the objection command, count limit, separate `room_end_vote` 廢村 request with same-day distinct-living-player majority finalization, persisted/broadcast notification, old-log transcript labeling, and optional browser beep for objection, phase/day changes, revotes, and sudden-death warnings instead of legacy SWF playback.
- Reference has silence/sudden-death checks in `game_play.php` flow. Current DO state now applies non-realtime silence acceleration when conversation resumes after the silence threshold; the DO alarm emits the reference-style final 2-minute warning, then sudden-deaths timed-out day voters and required night actors, resets action maps, and starts another same-phase deadline.
- Reference `GM_CHANNEL` can adjust several channel restrictions. Current GM controls cover kill, revive, role, flag, phase, winner declaration, whispers, common voice visibility, reference-style GM-kill vote/action reset, pending-action cleanup when life or role state changes, target-name persistence for old-log GM operations, a live GM action panel/status whose control rows stay hidden for non-GM viewers until websocket membership marks them as GM, PHP-shaped transcript labels for self-talk / GM whispers, and the `chdis:ch_wolf:ch_common:ch_lovers:ch_fox` restriction matrix; the admin menu now links filtered room inspection, D1-backed waste-room actions, BBS topic moderation entry points, runtime config, PHP-style `admin.php?go=status` status checks, and legacy `adpass` cookie room-admin compatibility, while broader server-management screens are still not fully mapped.
- Reference has resident exit/reset vote handling while waiting. Current lobby kick exists, players can voluntarily leave the lobby resident list with host reassignment, resident start votes follow the reference 8-player minimum and dummy-boy vote credit before auto-starting, and direct/vote-based kicks remove a target while resetting lobby votes and appending reference-style kick/reset log entries; remaining reset semantics may still differ from PHP.
- Reference has manual/auto refresh spectator view. Current app is realtime-first and now exposes PHP-style room page reload links with 15/20/30-second meta refresh options and addressable player/spectator/heaven room view modes with spectator/heaven-specific layout rows and a shared public player grid, but still does not fully split those views into separate frame-era templates.

## Next Concrete Work Items

1. Continue improving `/room/:roomId/log` toward PHP-compatible viewer-specific transcript masking; day/phase grouping, localized phase/winner payloads, historical vote tables, private talk/action transcript persistence, self-talk, old-log heaven/reverse display modes, player-view selectors, and major per-location styling are now in place.
2. Continue converting the remaining partial/missing rows in `docs/rule-parity-manifest.md` into focused tests and implementation slices.
3. Run `docs/visual-parity-checklist.md` and attach screenshots once a browser is available in the environment.
4. Continue reducing remaining legacy menu/page gaps; the admin index, legacy admin login/logout compatibility, and room-admin end screen exist, but other PHP-era server management pages are still not fully mapped.

## Current Verification Gaps

- No Playwright/browser E2E is installed or runnable in the current environment; `chromium`, `chromium-browser`, `google-chrome`, and `chrome` were not found.
- Local rendered UI smoke verifies HTTP-rendered pages but does not execute browser JavaScript.
- `docs/visual-parity-checklist.md` now defines the manual/browser evidence pass, but no screenshot baseline exists against the reference PHP UI.
